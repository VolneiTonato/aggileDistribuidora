module.exports = () => {

    global.config = global.config || {}

    global.config.configGeral = {
            template: {
                temas: ['sparklestore', 'persuit'],
                tema: 'persuit'
            }
        }
    }